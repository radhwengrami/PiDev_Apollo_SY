{{ form_start(form,{attr:{novalidate:'novalidate'}}) }}
    <div class="form-group">
            {{ form_label(form.idUser) }}
            {{ form_widget(form.idUser) }}
            {{ form_errors(form.idUser) }}
        </div>

        <div class="form-group">
            {{ form_label(form.oeuvres) }}
            {{ form_widget(form.oeuvres) }}
            {{ form_errors(form.oeuvres) }}
        </div>

        <div class="form-group">
            {{ form_label(form.accecible) }}
            {{ form_widget(form.accecible) }}
            {{ form_errors(form.accecible) }}
        </div>

        <button type="submit" class="btn btn-primary">Enregistrer</button>
    {{ form_end(form) }}
