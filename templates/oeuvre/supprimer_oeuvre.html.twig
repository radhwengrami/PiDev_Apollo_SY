<form method="post" action="{{ path('supprimer_Oeuvre', {'id': oeuvre_art.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ oeuvre_art.id) }}">
    <button class="btn">Delete</button>
</form>
