{{ form_start(form,{attr:{novalidate:'novalidate'}}) }}
    <div class="form-group">
            {{ form_label(form.titre) }}
            {{ form_widget(form.titre) }}
            {{ form_errors(form.titre) }}
        </div>

        <div class="form-group">
            {{ form_label(form.anne_creation) }}
            {{ form_widget(form.anne_creation) }}
            {{ form_errors(form.anne_creation) }}
        </div>

        <div class="form-group">
            {{ form_label(form.dimention) }}
            {{ form_widget(form.dimention) }}
            {{ form_errors(form.dimention) }}
        </div>

        <div class="form-group">
            {{ form_label(form.prix) }}
            {{ form_widget(form.prix) }}
            {{ form_errors(form.prix) }}
        </div>

        <div class="form-group">
            {{ form_label(form.disponibilite) }}
            {{ form_widget(form.disponibilite) }}
            {{ form_errors(form.disponibilite) }}
        </div>

        

        <button type="submit" class="btn btn-primary">Enregistrer</button>
    {{ form_end(form) }}
