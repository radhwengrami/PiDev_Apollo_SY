
<form method="post" action="{{ path('supprimer_Portfolio', {'id': portfolio.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ portfolio.id) }}">
    <button class="btn">Delete</button>
</form>
